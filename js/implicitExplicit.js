


//! implicit return: when 

// const multiply = (a, b) => a * b; // Implicit return
// console.log(multiply(2, 3)); // Output: 6


// const getUser = (name, age) => ({ name, age }); // Parentheses for object
// console.log(getUser("Alice", 25)); // Output: { name: 'Alice', age: 25 }





// ! An explicit return means the return keyword is explicitly used to specify the value being returned. It is used in both traditional functions and arrow functions with a block body.

const demo = (a,b) => {

    // return a+b;
    // console.log(a+b)
}

console.log(demo(10,20));